# CNT Lab Web UI 设计规范

门户与各分析工具使用同一套“科研纸面 / 仪器舱”视觉系统。各项目独立部署，不依赖跨域 CSS；本文件是颜色、间距和主题行为的规范源。

## 文件职责

门户采用两层样式，**同一条规则只在一个文件里定义**，避免同一选择器被两个文件分段覆盖：

| 文件 | 负责 |
| --- | --- |
| `theme.css` | 颜色变量、深色模式覆盖、品牌资产、状态语义色、图标配色 |
| `styles.css` | 版式、尺寸、间距、断点、组件布局 |

## 主题

- 主题键：`cnt-lab-theme`
- 可选值：`light`、`dark`
- 未保存主题时跟随 `prefers-color-scheme`
- 用户手动切换后写入当前站点的 `localStorage`
- 所有页面在 `<html>` 上使用 `data-theme="light|dark"`
- 专业图像画布始终保持深色，以维持图像和标注对比度

## 核心色彩

| 角色 | 浅色 | 深色 |
| --- | --- | --- |
| 页面背景 | `#f3f5f0` | `#081a18` |
| 主表面 | `#ffffff` | `#102724` |
| 次表面 | `#edf2ee` | `#16322e` |
| 主文字 | `#17322e` | `#edf7f3` |
| 次文字 | `#55685f` | `#9aada7` |
| 边框 | `#d8e1dc` | `#29453f` |
| 品牌青绿 | `#0b7a70` | `#68d3c4` |
| 强调黄绿 | `#cdea72` | `#ccec70` |
| 告警珊瑚 | `#f17b64` | `#ff8a72` |
| 琥珀 | `#d99b42` | `#efb65f` |

次文字是小字号（12 px）说明文字的主要颜色，**两种模式下都必须保持 ≥ 4.5:1 对比度**。浅色值由 `#637770` 加深为 `#55685f`，因为前者在页面底色上只有约 4.1:1。

## 状态语义色

工具卡片右上角的接口状态胶囊。中性态（`unknown`）用次文字色 + 次表面，三态色定义在 `theme.css`：

| 状态 | 浅色前景 / 底色 | 深色前景 / 底色 |
| --- | --- | --- |
| online | `#2f6244` / `#e7f3e9` | `#9fe1b3` / `rgba(86,174,113,.14)` |
| checking | `#7a5518` / `#fff3da` | `#f2ca7b` / `rgba(235,168,73,.14)` |
| offline | `#9e4436` / `#fff0eb` | `#ffab9c` / `rgba(237,116,92,.14)` |
| unknown | 次文字 / 次表面 | 次文字 / 次表面 |

状态只反映浏览器侧探测结果，**不得表述为“工具可用 / 不可用”**；解释文案必须与概览数字同区域呈现，不能放在页面底部（原先两者在垂直方向相距约 3000 px）。

## 工具图标色

卡片图标是页面上颜色最密集的区域。浅色底 + 深图形，共 9 组，深色模式统一反转为深底 + 浅图形：

| 图标 | 浅色底 / 图形 | 深色底 / 图形 |
| --- | --- | --- |
| optical | `#dcf1a4` / `#226c54` | `#2a3618` / `#b5d96a` |
| tem | `#fff0eb` / `#ee7c66` | `#3a2620` / `#f0917c` |
| sem | `#e8f0f8` / `#4a7fa8` | `#1e2c38` / `#7aa8c8` |
| grain | `#eeeafd` / `#7767bf` | `#2a2540` / `#a99ae0` |
| uv | `#edf7e1`→`#f5e9fa` / `#458d77`+`#d3729b` | `#25301d`→`#2d2437` / `#6fb89b`+`#d987ad` |
| raman | `#e7f1f7` / `#3f7898` | `#1d2e3a` / `#7aa9c4` |
| bet | `#f8efdb` / `#d9a341` | `#332b1a` / `#e0b463` |
| production | `#e7edf3` / `#5c7891` | `#222c34` / `#8fa8bd` |
| vision | `#cdea72` / `#163e30` | 同浅色（位于深绿主推卡片内） |

**深色模式必须显式覆盖，不能只靠 `filter: brightness()`**——浅色底在深色页面上会成为亮度最高的元素，把注意力从工具名上抢走。

## 排版与布局

- 正文字体：`Inter / Segoe UI / Microsoft YaHei / system-ui`
- 页面主标题：`Georgia / Noto Serif SC / serif`
- 间距统一为 4 px 的倍数，主要组件使用 8 / 12 / 16 / 24 / 32 px
- 卡片间距（24 px）不小于卡片内边距（24 px），避免卡片互相“粘连”
- 分组之间的垂直间距约为组内标题到卡片的 3 倍，以形成清晰的分组边界
- 主卡片圆角 18 px，控件圆角 8–10 px
- 桌面工作区优先采用“参数栏 + 结果区”；移动端按操作顺序纵向排列
- 数据表和图表必须允许横向滚动，不以缩小文字换取强行塞入
- **标题层级不得倒置**：分组副标题与卡片描述同为 16 px，均不小于其下级文字
- 卡片标题用 `clamp()` 随视口缩放，避免窄屏出现末行只剩一两个字的孤字行

## 断点

| 宽度 | 工具网格 | 说明 |
| --- | --- | --- |
| > 1100 px | 3 列 | — |
| 901–1100 px | 2 列 | 图像组第 3 张卡片跨满整行 |
| 681–900 px | 2 列 | 主推卡片转为两行布局 |
| 431–680 px | 1 列 | — |
| ≤ 430 px | 1 列 | 主推卡片单列并隐藏其装饰图标 |
| ≤ 340 px | 1 列 | 主题开关收为纯图标 |

断点归属必须唯一。此前 1100 px 的列数规则同时写在 `theme.css` 与 `styles.css`，后者覆盖了前者，导致 901–1100 px 区间仍是 3 列、卡片内宽不足 250 px、描述文字末行只剩一个字。

## 交互约束

- 主题开关在页面右上区域；移动端保留可见文字标签，仅在 340 px 以下收为纯图标
- **所有可点击控件的命中区域不小于 44 × 44 px**。纯文字控件用 `::after` 伪元素外扩热区，不用内边距撑大排版
- 所有可点击控件保留 `:focus-visible` 状态
- 禁用状态同时通过颜色和光标表达
- 支持 `prefers-reduced-motion`
- 主题切换不得清空文件、参数、分析结果或登录状态
- 同一页面内的按钮文案需可区分，避免多个“打开分析台”指向不同工具
- 主题开关的可见文字与 `aria-label` 描述的都是“点击后的目标主题”，因此不再设置 `aria-pressed`，避免用当前状态描述同一个控件

## 品牌资产

`assets/cnano-logo.png` 的 “nano” 字为深灰色。深色模式必须为其提供浅色底片（当前做法），或另备反白版；直接用 `filter` 提亮会同时改变品牌绿。
